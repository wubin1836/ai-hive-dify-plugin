[project]
name = "ai-hive-aigc"
version = "0.1.0"
description = "AI Hive image and video generation tools for Dify"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "dify_plugin>=0.9.0",
  "requests>=2.32.0,<3",
]

[dependency-groups]
dev = []
